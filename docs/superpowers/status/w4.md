# Welle 4 — Status: Outreach als kontrollierte Abschlussstrecke

**Plan:** [`2026-08-20-leadgen-vollausbau-gsd.md`](../plans/2026-08-20-leadgen-vollausbau-gsd.md) · **Branch:** `gsd-vollausbau`
**Ziel:** Assistenz für einen Menschen — verknüpft Lead, geprüften Nutzen und ggf. freigegebene Demo, ohne Versandentscheidungen zu automatisieren.

## Pakete (Owner: `outreach-engineer`)

| Paket | Ergebnis |
|---|---|
| W4.1 Kontakt-Readiness | `outreach_readiness(lead)` → `{ok, fehlend, demo_link}`. Pflichtpunkte: warm/kalt-freigegeben, E-Mail, Anlass, Angebot, Nutzen, CTA. Cockpit gatet den Button, nennt den ersten fehlenden Punkt. |
| W4.2 Entwurfskontext | Demo-Link **nur** bei `prototyp_state.status == "published"`; `draft_ready`/`approved_local` liefern keinen Link. `save_request()` entfernt einen Demo-Link, wenn nicht published. Löst den W3-losen-Faden. |
| W4.3 Einzel-Freigabe | `validate_send()` wirft bei fehlendem Entwurf, Doppelversand (bereits `sent`), fehlender E-Mail. Vorschau zeigt Empfänger/Betreff/Text/Link/Sendemodus. `draft` Default, `direct` nur nach expliziter Dropdown-Auswahl. Tests mit injiziertem FakeSMTP — kein echter Versand. |

## Gate W4: GRÜN ✅
`python -m pytest -q` → **292 passed** (+25 W4-Tests: 13 Readiness, 6 Entwurfskontext, 6 Einzelfreigabe). Eine Mail kann vollständig vorbereitet/geprüft werden, aber nie aus Batch/Scheduler/ohne Freigabeschritt versandt werden.

## Offene Punkte
- **W4.4 Nachbereitung** (Status/Kontaktzeit/Wiedervorlage nach echter Entscheidung via CLI/API,
  `pipeline-operator`) — im End-to-End-Pilot (Welle 6) verifiziert, kein neuer Code nötig
  (nutzt bestehendes `mark_contacted`/`set_wiedervorlage`).
- Lead-Schema hat kein eigenes `anlass`-Feld; `outreach_readiness` fällt auf `schwaeche` zurück.
  Manuell angelegte Kalt-Leads ohne Schwäche zeigen dann „Anlass fehlt". Bewusst offen (YAGNI) —
  erst ein eigenes Feld einführen, wenn der Pilot es verlangt.
- `angebot`/`nutzen` werden aus `ucp`/`roi_these` abgeleitet → zwingt zur Datenpflege vor dem
  Anschreiben (gewollt).