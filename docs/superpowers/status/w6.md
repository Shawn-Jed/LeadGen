# Welle 6 — Status: End-to-End-Pilot + Konsolidierung

**Plan:** [`2026-08-20-leadgen-vollausbau-gsd.md`](../plans/2026-08-20-leadgen-vollausbau-gsd.md) · **Branch:** `gsd-vollausbau`

## End-to-End-Smoke (API, eigener Port 8799)

| Schritt | Ergebnis |
|---|---|
| `/api/state` | 62 Leads, 18 Runs, Priorisierung/Report vorhanden |
| Prototyp `request → draft → approve` | `pending → draft_ready → approved_local` |
| Nach `approve`: `url == null` | ✅ **kein Auto-Publish** — öffentlicher Link nur über bewussten publish-Schritt |
| Outreach-Readiness | `ok=false` + klare Fehlliste (gatet korrekt) |
| `/api/portfolio` | `{schema_version:1, eintraege:[]}` (Empty-State) |

**Wichtig:** Port 8723 wird von Shawns parallel laufendem Cockpit belegt — E2E daher auf 8799
gegen den aktuellen Code. Kein Außenwirkungsschritt (Publish/Versand) ausgelöst.

## Abschluss-Review (`code-qualitaet`, Gesamt-Diff e1a30bb..HEAD)

**Verdikt: OK zum Mergen** (kleine Fixes, nicht blockiert). Alle Sicherheits- (Slug-Validierung,
kein Traversal, kein Secret, Publish-/Send-Guards) und Konventions-Checks (CLI-only CRM, utf-8,
Statusmodell) bestanden. **Behobene 🟡-Befunde:**
- `outreach.py`: kaputter f-String in `validate_send` (Statuswert wurde literal ausgegeben).
- `app.py` Publish: Guard gegen leeres HTML (kein Deploy einer leeren Seite im Migrations-Edge-Case).
- `app.py` Readiness: warme Leads wurden immer als „kalt" gewertet (`warm` steht nicht im
  Frontmatter) → jetzt aus Dateiexistenz abgeleitet; totes `ps_data` entfernt.

Offen/bewusst belassen (🟢): `artefaktpfad` unvalidiert (kein POST-Endpunkt → kein Risiko;
Guard bei späterem Endpoint), `dv_wert`-Skalierung (Designentscheidung, getestet).

## Gate: 294 passed

## Definition of Done (Plan §9)

- [x] Klar abgegrenztes Segment mit kleiner Kandidatenmenge (Pflegedienst Winterhude/Barmbek, ≤8 → 3 Leads).
- [x] Jeder übernommene Pilot-Lead hat nachvollziehbare Schwäche + Evidenz (Notizen mit Quelle).
- [x] Cockpit zeigt erklärbare nächste Aktion (Fokus-Tab, Faktor-Breakdown) statt nur Datenliste.
- [x] Demo durchläuft `pending → … → approved_local` inkl. Technik-/Faktenbindung (A/B, self-contained). *(Foto/Zitat = markierte Platzhalter; Mobil-375px im echten Browser noch offen.)*
- [x] Öffentliche Demo nur nach expliziter Einzelentscheidung, im Zustand sichtbar (publish-Guard, E2E: url bleibt null).
- [x] Outreach-Entwurf mit vollständigem Kontext vorbereitbar; Versand braucht sichtbar Einzelbestätigung (`draft` Default, `direct` opt-in, Doppelversand/Adresse geprüft).
- [~] Mindestens ein Portfolio-Muster gesichert — **Struktur fertig, Startbedingung ≥2 freigegebene Demos im Pilot noch nicht erreicht** (Manifest bewusst leer).
- [x] Relevante Test-Suite tatsächlich ausgeführt (294); Änderungen durch Code-Review geprüft.
- [x] Keine CRM-Datei außerhalb CLI/API editiert; keine Zugangsdaten versioniert.

## Belegter Engpass (Plan §9, Schlussfrage)
Der reale Engpass im Pilot ist **Kandidatenqualität/Datenabdeckung**: OSM (`social_facility`)
liefert viel Rauschen und dünne Abdeckung im Pflege-Segment; echte Dienste fehlen in OSM.
Nächste Ausbaustufe sollte hier ansetzen (bessere Quellen/Gegencheck), nicht bei mehr
Automatisierung. Zweitgrößter offener Punkt: visuelle 375-px-Bestätigung der Demos im echten
Browser (Playwright im Pilot nicht durchgekommen).

## Nicht abgeschlossen (ehrlich)
- W1.2/W1.3 (Discovery-Schema/Triage-Engineering) bewusst zurückgestellt — bei 3 Leads kein Engpass.
- W4.4 Nachbereitung: nutzt bestehendes `mark_contacted`/`set_wiedervorlage`; im echten Kontakt-Fall zu verifizieren.
- Portfolio-POST-Endpoint + echte Einträge: erst wenn ≥2 Demos freigegeben.