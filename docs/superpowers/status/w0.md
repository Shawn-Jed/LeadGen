# Welle 0 — Status

**Plan:** [`2026-08-20-leadgen-vollausbau-gsd.md`](../plans/2026-08-20-leadgen-vollausbau-gsd.md)
**Branch:** `gsd-vollausbau`
**Ziel der Welle:** Reproduzierbarer Ausgangspunkt, sichere Arbeitsumgebung und ein bestätigter Pilotvertrag — ohne Lead-Änderung, ohne Versand, ohne Veröffentlichung.
**Hypothese:** Die Engstelle ist nicht die Lead-Menge, sondern ein durchgängiger, prüfbarer Pfad von Kandidat → Demo → Anschreiben → Portfolio.

## Pakete & Dateieigentümer

| Paket | Eigentümer | Schreibbereich | Status |
|---|---|---|---|
| W0.1 Baseline | `gsd-orchestrator` | Plan-/Statusdocs | ✅ erledigt |
| W0.2 Agenten ergänzen | `gsd-orchestrator` | `.claude/agents/*` | ✅ erledigt |
| W0.3 Produktvertrag | Orchestrator + Shawn | Statusdoc | ✅ bestätigt |
| W0.4 Datenhygiene | `discovery-auditor` | keiner (Bericht) | ✅ erledigt (Bericht unten) |

## W0.1 Baseline (Nachweis)

- **Branch:** `gsd-vollausbau` von `main` (e1a30bb) angelegt, Arbeitstree zu Beginn sauber.
- **Test-Suite:** `cd backend && python -m pytest -q` → **142 passed in ~3.6s**.
- **Cockpit-Start:** `cd backend && python app.py` → http://127.0.0.1:8723 (Smoke-Protokoll siehe unten).
- **Bekannte Abweichung (zu lösen in Welle 3):** Subsystem C hat zwei parallele Wege —
  im Cockpit den manuellen „Design-Prompt kopieren"-Weg, im Backend/Plan den
  Auftrag-zu-Deploy-Weg. Der frühere Demo-Status `ready` ist nicht in `draft_ready` vs.
  `published` getrennt. Welle 3 vereinheitlicht das auf die kanonische Zustandsmaschine (2.1).

## W0.2 Agenten (angelegt unter `.claude/agents/`)

`gsd-orchestrator`, `discovery-auditor`, `discovery-engineer`, `crm-cockpit-engineer`,
`prototype-art-director`, `prototype-engineer`, `prototype-reviewer`, `outreach-engineer`,
`portfolio-curator`, `test-engineer`. Bestehend/wiederverwendet: `lead-anlegen`
(= `pipeline-operator`), `code-qualitaet`, `stakeholder-tester`. Keine Doppelzuständigkeit auf
einer Produktionsdatei.

## W0.3 Pilotvertrag (bestätigt 2026-08-20)

| Feld | Entscheidung |
|---|---|
| Fokussegment | **Pflegedienst** (vom Orchestrator gewählt — Shawn: „egal"; passt zu Bereich + Relaunch, Discovery-Daten vorhanden) |
| Lokaler Bereich | **Winterhude / Barmbek** |
| Angebot | **Website-Relaunch** zum Festpreis (Ersatz veralteter Mehrseiten-Auftritte) |
| Primärer Kundennutzen | Ein moderner, mobiler, vertrauenswürdiger Auftritt, der Angehörige und Bewerber überzeugt |
| CTA | **Live-Demo-Link ansehen** — Voraussetzung: Demo-Status `published` (Welle 3, Einzelfreigabe). Fallback bis dahin: 15-Minuten-Telefonat |
| Wellenlimit | max. 8 Kandidaten → max. 3 qualifizierte Leads → max. 1 Demo |
| Demo-Kriterien | nachvollziehbare Schwäche, ausreichende Fakten, plausibler wirtschaftlicher Nutzen |

> **Abhängigkeit CTA:** „Live-Demo-Link" ist erst nutzbar, wenn eine Demo `approved_local`
> → `published` durchlaufen hat (menschliche Einzelfreigabe, W3.5/W3.6). Solange keine
> veröffentlichte Demo existiert, nutzt Outreach die Telefonat-CTA.

## Akzeptanzkriterien Welle 0

- [x] Reproduzierbare Baseline mit echter Testausgabe dokumentiert.
- [x] Fehlende Agentendefinitionen angelegt, schmale Dateigrenzen, kein Overlap.
- [x] Pilotvertrag bestätigt (Segment, Bereich, Angebot, Nutzen, CTA, Limit).
- [x] Datenhygiene-Bericht mit `behalten`/`nachprüfen`/`archivieren`, ohne stille Mutation.

**➡ Welle-0-Gate: GRÜN.** Baseline reproduzierbar, Agenten angelegt, Pilotvertrag bestätigt, Datenhygiene-Audit ohne Mutation abgeschlossen. Welle 1 ist startklar (benötigt Netzzugriff).

## W0.4 Datenhygiene — Ergebnis (discovery-auditor, read-only)

**Systemzustand:** 16 Discovery-Runs (davon 4 Pflegedienst: Barmbek-Nord, Steilshoop,
Ohlsdorf, Bramfeld), 0 warme Leads, 65 kalte Pipeline-Einträge — **62 davon in
`identifiziert`** (Triage-Engpass). **Kein Pflegedienst-Eintrag in `pipeline.md`.**

**Pflegedienst-Kandidaten:** kein einziger mit dokumentierter Website-*Schwäche*
(`score` pauschal 0/60, `befund` nur „Website in OSM (nicht) hinterlegt"). Viele
Barmbek-Nord-Treffer sind **falsches Segment** (Jugend-/Sozialhilfe). Ergebnis Barmbek-Nord:
6 `nachprüfen`, 10 `archivieren`, 0 `behalten`. Duplikat entdeckt: „St. Gabriel" in
Barmbek-Nord **und** Steilshoop.

**Winterhude-Lücke:** **kein** Pflegedienst-Run für Winterhude → Fokusbereich nur zur Hälfte
abgedeckt.

**Pipeline-Hygiene (nicht blockierend):** 8 Einträge (angel-extensions, hansebarbier,
schnittpunkt, haar-scharf, orchideen-palast, daddy-pizza, restaurant-alexandros,
indian-temple) haben URL in der Notiz, aber leeres Website-Feld + Status `identifiziert` →
`nachprüfen`. `salon-48`: Widerspruch Notiz vs. Schwäche-Feld. `zahnarztpraxis-dr-behrens`:
kontaktiert 2026-06-30, Wiedervorlage fällig. `behalten`: `style-of-hair` (inaktiv,
dokumentiert), `minnemann-elektrotechnik` (Prototyp live).

**Empfehlung für W1 (Reihenfolge):**
1. **Scan `pflegedienst Winterhude`** — fehlt komplett, höchste Priorität.
2. **Gegencheck der 6 Barmbek-Nord `website_unklar`** (Pflege mit Verständnis, Renafan,
   alsterdorf assistenz ost, Integra Seniorenpflegezentrum, Rungehaus, St. Gabriel) per
   WebSearch → Qualitätsbefund oder archivieren.
3. **Wellenselektion:** 8 validierte Kandidaten mit echtem Pflegedienst-Charakter **und**
   nachweislich schwacher Website. Aktuell 0 → erst nach Gegencheck befüllbar.
4. Pipeline-Hygiene parallel, kein W1-Blocker.

## Risiken

- Ohne bestätigten Pilotvertrag darf Welle 1 nicht starten (Stoppkriterium Abschnitt 8).
- Shawn nutzt das Cockpit parallel; Datendateien (`pipeline.md`, `discovery/*.json`) können
  sich mitten in der Session ändern → nicht zurücksetzen, read-only auditieren.

## Cockpit-Smoke-Protokoll

- `python app.py --api-only` startet ohne Fehler.
- `GET /api/state` → **HTTP 200**; Keys `today, statuses, leads, report, discovery_runs`.
- Datenstand zum Baseline-Zeitpunkt: **59 Leads**, **16 Discovery-Runs**.
- API-Routen bestätigt (Auszug): `/api/state`, `/api/discovery/*`, `/api/leads/<slug>/status|note|wiedervorlage`, `/api/prototyp/pending`, `/api/leads/<slug>/prototyp/(request|draft)`, `/api/outreach/pending`, `/api/leads/<slug>/outreach/(request|draft|send)`.
