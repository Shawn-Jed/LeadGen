# Welle 2 — Status: CRM als Arbeitsoberfläche für die nächste beste Aktion

**Plan:** [`2026-08-20-leadgen-vollausbau-gsd.md`](../plans/2026-08-20-leadgen-vollausbau-gsd.md) · **Branch:** `gsd-vollausbau`
**Ziel:** Klare Tagesansicht mit erklärbarer Priorisierung und genau einer nächsten Aktion pro Lead.

## Pakete (Owner: `crm-cockpit-engineer`, Gates: `code-qualitaet` + `test-engineer`)

| Paket | Ergebnis |
|---|---|
| W2.1 Entscheidungsmodell | `priority_score(lead, today)` in `leadtool.py` — 4 sichtbare Faktoren (Befundstärke, Segmentpassung, Datenvollständigkeit, fällige Wiedervorlage), je mit Wert + Erklärung, Summe max 12. Reine Funktion, kein Seiteneffekt. |
| W2.2 Nächste Aktion | `next_action(lead, today)` → einer aus {pruefen, qualifizieren, demo_beauftragen, kontaktieren, nachfassen}. Fällige Wiedervorlage dominiert. In `/api/state` verdrahtet (`build_state` reichert jeden Lead an), im Cockpit als `na-badge`. |
| W2.3 Datenintegrität | `test-engineer`: +41 Negativtests (ungültige Statussprünge, Graduierung/kein Rückfall, doppelte Slugs, Wiedervorlage-Formate, Notiz-Encoding, report/priority/next_action-Randfälle). Keine Produktions-Bugs gefunden. |
| W2.4 Fokusansicht | Neuer Tab „Fokus": „Heute / Fällig" + „Diese Woche — Top-Leads" (max 5 nach Score), Klick öffnet Drawer mit Faktor-Breakdown. Kein Kalender, kein Hintergrunddienst. |

## Gates

- **Test-Gate:** `python -m pytest -q` → **224 passed** (142 Bestand + 41 W2-TDD + 41 W2.3).
- **Integritäts-Gate (`code-qualitaet`):** Verdikt **OK zum Mergen**. 3 nicht-blockierende Befunde;
  die zwei 🟡 (toter Code `dv_punkte`, Docstring/Logik-Widerspruch Segmentpassung) direkt behoben.
  🟢 (String-Datumsvergleich in `app.js`) belassen — funktioniert, nur Härtungshinweis.
- **UX-Gate:** auf den konsolidierten Stakeholder-Test in Welle 6 verschoben (Server-Start gebündelt).

## Gate W2: GRÜN ✅
Priorität ist erklärbar (jeder Faktor sichtbar), pro Lead genau eine nächste Aktion, keine
automatische Status-/Kontaktentscheidung. 14-Tage-Regel bleibt Vorschlag.

## Offene Punkte
- Segmentpassung kalter Leads bleibt schwach, weil `pipeline.md` keine `branche`-Spalte führt
  (bewusst YAGNI — erst bei Bedarf ein eigenes Paket).
- Offline-Mock in `app.js` zeigt keine Badges (kein Fehler, nur Fallback-Optik).